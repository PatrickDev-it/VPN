[vpn]
# Populated dynamically from Terraform output:
#   TF_IP=$(cd infra/terraform/aws && terraform output -raw public_ip)
#   ansible-playbook -i "infra/ansible/inventories/aws/hosts.ini" infra/ansible/aws.yml -e "ansible_host=$TF_IP"
vpn-server ansible_host=REPLACE_WITH_EC2_IP ansible_user=ubuntu
